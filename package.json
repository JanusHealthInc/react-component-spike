{
  "name": "react-component-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:bootstrap": "cd src/bootstrap && yarn build",
    "build:material-ui": "cd src/material-ui && yarn build",
    "build:tailwind": "cd src/tailwind && yarn build",
    "build": "yarn && tsc && vite build",
    "build-all": "yarn build:bootstrap && yarn build:material-ui && yarn build:tailwind && build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "@vitejs/plugin-react": "^3.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  }
}
